/* eslint-disable @typescript-eslint/no-unused-vars */
type TokenPayload = {
  sub: number;
  email: string;
  username: string;
};

type UserRequest = {
  id: number;
  email: string;
  username: string;
};
